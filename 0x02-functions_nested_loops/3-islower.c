#include "main.h"

/**
 *_islower(int c) - prints 10 times the alphabet in lowercase
 *
 *return: 0
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
