#include "main.h"

/**
 *add - adds two integers and returns the result
 *@x: two integer arguments
 *@y: twi integer arguments
 *
 *Return: sum of the two integers
 */

int add(int x, int y)
{
	int sum = x + y;
	return(sum);
}
