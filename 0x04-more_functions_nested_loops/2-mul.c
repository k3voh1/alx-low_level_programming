#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers.
 * @a: integer arguement
 * @b: integer arguement
 * eturn: Always 0 (success)
 */

int mul(int a, int b)
{
	return (a*b);
}
